name: Call parent script
description: Call a script in a parent directory
inputs:
  name:
    description: "Name"
    required: true
    default: "Jeff"
runs:
  using: composite
  steps:
    - name: Call parent script
      shell: pwsh
      run: ${{ github.action_path }}/scripts/test.ps1 ${{ inputs.name }}
